# Remotion Reloaded Starter Guidance

## Commands
- `npm run preview`
- `npm run build`
- `npm run render`

## Defaults
- `remotion.config.ts` already uses `withReloaded()`.
- Example compositions are in `src/compositions/`.
- Keep animations deterministic for frame seeks.
